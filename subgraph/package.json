{
    "name": "Dapp Tools",
    "license": "UNLICENSED",
    "scripts": {
        "prepare": "graph codegen && graph build --network mumbai --network-file networks.json",
        "codegen": "graph codegen",
        "build-polygon": "graph build --network matic --network-file networks.json",
        "build-mumbai": "graph build --network mumbai --network-file networks.json",
        "auth": "graph auth --product hosted-service",
        "deploy": "graph deploy --product hosted-service erroders/dapp-tools"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.30.4",
        "@graphprotocol/graph-ts": "^0.27.0",
        "@openzeppelin/subgraphs": "^0.1.8"
    }
}
